package alura.basics.imports;

//import alura.basics.imports.Endereco; => opcional quando a classe está dentro do mesmo pacote

public class Pessoa {
    String nome;
    Endereco endereco; // se está no mesmo pacote, não existe a necessidade de adicionar um import
}
